# ---------------------------------------------------------------------------
# Backend API
# ---------------------------------------------------------------------------
# En desarrollo: Vite hace proxy de /api a localhost:3000 (vite.config.ts).
# No hace falta definir variable; las peticiones van a /api/v1 y el proxy las envía al backend.
# En producción: configurar la URL base del backend desplegado donde corresponda.

# ---------------------------------------------------------------------------
# Socket.IO (notificaciones de transacción)
# ---------------------------------------------------------------------------
# En desarrollo: URL del backend donde corre Socket.IO (ej. http://localhost:3000).
# En producción: URL del backend desplegado (ej. https://api.tudominio.com).
VITE_SOCKET_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Wompi Sandbox (solo se usa la llave pública en el frontend)
# ---------------------------------------------------------------------------
VITE_WOMPI_API_URL=https://api-sandbox.co.uat.wompi.dev/v1
VITE_WOMPI_PUBLIC_KEY=pub_stagtest_g2u0HQd3ZMh05hsSgTS2lUV8t3s4mOt7

# Fees (deben coincidir con el backend; en centavos COP)
VITE_BASE_FEE_CENTS=0
VITE_DELIVERY_FEE_CENTS=0
